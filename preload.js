// Minimal preload — reserved for future bridge between dashboard and WhatsApp.
// Kept intentionally tiny; no privileged APIs are exposed for now.
window.addEventListener('DOMContentLoaded', () => {});
